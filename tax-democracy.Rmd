---
output: pdf_document
---
hek---
title: "Taxation and Democracy"
author: "Maximilian Held"
classoption: a4paper
output:
  pdf_document:
    fig_caption: yes
    keep_tex: yes
    number_sections: yes
    toc: yes
geometry: margin=1.5in
documentclass : memoir
lang: english
bibliography: ../held-library/held_library.bib
---

```{r preamble, echo = FALSE}
library(devtools)
library(knitr)
library(rmarkdown)
library(grid)  # needed, for, well, grids and arrows
library(gridExtra)  # needed for more grids
library(reshape2)
library(plyr)
library(ggplot2)
install.packages(repos = NULL, type = "source", INSTALL_opts = c('--no-lock'), pkgs = c("qmethod"))
library(qmethod)
```

```{r setup-knitr, cache = FALSE, include = FALSE}
opts_knit$set(root.dir = normalizePath(getwd()))  # what does this do?
options(digits=2)
getwd()
```

```{r q-method, child="method-q.Rmd"}
```

```{r results-factors, child="results-factors.Rmd"}
```


# References
